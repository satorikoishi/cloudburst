ip: 127.0.0.1
mgmt_ip: NULL
user_states:
  # A default item named `anna` will be automatically added when running. 
  # This is because `anna_ipc` must be created at runtime
  # So the items bellow are just the additional KVSs that can be used.
  - name: shredder
    type: shredder
    host: 10.10.1.2
    port: 6379
executor:
  scheduler_ips:
    - 127.0.0.1
  thread_id: 0
scheduler:
  routing_address: 127.0.0.1
  metric_address: 127.0.0.1
  policy: locality
benchmark:
  cloudburst_address: 127.0.0.1
  thread_id: 0
